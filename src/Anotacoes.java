public class Anotacoes {
    public static void main(String[] args) {
/*
    OPERAÇÕES:

        int a = 10; int b =5;
        System.out.println(a + b); // soma
        System.out.println(a - b); // subtração
        System.out.println(a * b); // multiplicação
        System.out.println(a / b); // divisão inteira
        System.out.println(a % b); // resto da divisão

        "==" igual a
        "!=" diferente de
        ">" maior que
        ">=" maior ou igual a
        "<" (menor que)
        "<=" menor ou igual a

    CONVENÇÕES DE CÓDIGO JAVA:

        NOMES DE CLASSES: PascalCase (ou Upper CamelCase). ex: MinhaClasse
        NOMES DE MÉTODOS: camelCase. ex: meuMetodo()
        NOMES DE CONSTANTES: todas maiúsculas separadas por underline. ex: MINHA_CONSTANTE
        NOMES DE VARIÁVEIS: camelCase. ex: minhaVariavel

    TIPOS PRIMITIVOS:

        BOOLEAN: true ou false
        BYTE: valores numéricos inteiros de 8 bits. valores de -128 a 127
        CHAR: caracteres individuais. pode armazenar qualquer caractere Unicode e é representado por aspas simples ('')
        SHORT: valores numéricos inteiros de 16 bits. valores de -32.768 a 32.767
        INT: valores numéricos inteiros de 32 bits. um dos mais usados para números inteiros. valores de -2.147.483.648 a 2.147.483.647
        LONG: valores numéricos inteiros de 64 bits. valores inteiros muito grandes. valores de -9.223.372.036.854.775.808 a 9.223.372.036.854.775.807
        FLOAT: números decimais com até 7 dígitos. ocupa 32 bits de memória. tem precisão limitada (pode arredondar os números se eles forem muito grandes ou muito pequenos)
        DOUBLE: números decimais com até 15 dígitos. ocupa 64 bits de memória.

    FORMATAÇÃO:

        metodo format(), da classe String. usa placeholders representados pelo caractere % seguido de uma letra que indica o tipo de dado que será inserido no placeholder
        ex: %s para string, %d para valor inteiro e %f para ponto flutuante (%.2f para mostrar apenas 2 casas decimais)

        String nome = "Maria";
        int idade = 30;
        double valor = 55.9999;
        System.out.println(String.format("Meu nome é %s, eu tenho %d anos e hoje gastei %.2f reais", nome, idade, valor));

        String nome = "João";
        int aulas = 4;
        String mensagem = """
                        Olá, %s!
                        Boas vindas ao curso de Java.
                        Teremos %d aulas para te mostrar o que é preciso para você dar o seu primeiro mergulho na linguagem!
                         """.formatted(nome, aulas);
        System.out.println(mensagem);

    CASTING:

        converter um tipo de dado em outro. pode ser feita de forma automática pelo compilador (conversão implícita), quando o tipo de dado de destino é compatível com o tipo de dado de origem, ou de forma manual (conversão explícita), utilizando o operador de casting

        Casting implícito: realizado automaticamente pelo compilador quando o tipo de dado de origem é compatível com o tipo de dado de destino
        int x = 10;
        double y = x; // casting implícito

        Casting explícito: realizado quando o tipo de dado de origem é incompatível com o tipo de dado de destino. usar o operador de casting para realizar a conversão
        double x = 10.5;
        int y = (int) x; // casting explícito

    LOOP WHILE

        int contador = 1;
            while(contador <= 10) {
                System.out.println(contador); contador++;
            }

    LOOP FOR
        int contador = 1;
        for(int contador = 1; contador <= 10; contador++) {
            System.out.println(contador);
        }
*/

    }
}